# Cloud Functions environment (Firebase loads functions/.env automatically).
# Copy this file to `functions/.env` and fill in your own values. It is gitignored.

# Comma-separated list of the Google account email(s) allowed to use YOUR ZeroTide.
# Only these signed-in accounts can read or change anything. Required.
ZEROTIDE_OWNER_EMAILS=you@example.com
